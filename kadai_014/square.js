// 数値を二乗する関数
const calculateSquare = (number) => {
  // 数値を二乗してその結果を戻り値として返す
  return number * number;
};

// 関数を使用して10の二乗を計算
const result = calculateSquare(10);

// 結果をコンソールに表示
console.log(result); // 出力: 100